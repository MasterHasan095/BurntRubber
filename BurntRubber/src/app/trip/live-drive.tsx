import { Text, View } from "react-native";

export default function LiveDrive() {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Live Drive — TODO</Text>
    </View>
  );
}
